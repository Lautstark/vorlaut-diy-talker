# Azure Speech: Schlüssel und Region müssen zusammenpassen.
# Ein kostenloses Konto genügt; die Stufe F0 enthält 0,5 Mio. Zeichen im Monat.
AZURE_SPEECH_KEY=hier-den-eigenen-schluessel-eintragen

# Region des Schlüssels. Steht im Azure-Portal bei der Speech-Ressource.
AZURE_SPEECH_REGION=germanywestcentral

# Optional. Verfügbare Stimmen zeigt:  python tts.py --stimmen
# Die Sprache wird aus dem Namen abgeleitet (de-DE-GiselaNeural -> de-DE).
#AZURE_SPEECH_VOICE=de-DE-GiselaNeural

# Optional. Sprechtempo, z. B. -5% oder +10%.
#AZURE_SPEECH_RATE=-5%

# Optional. Pfad auf eine lizenzierte METACOM-Sammlung (entpackter Download).
# Die Symbole werden nur verwiesen, nicht ins Projekt kopiert.
#VORLAUT_METACOM_DIR=/Users/du/METACOM_9_Desktop

# Optional. Schluessel fuer den Abgleich mit dem Talker. Ohne diesen Eintrag
# bleiben die Geraete-Endpunkte zu - dort liegen die Tonaufnahmen und Bilder.
# Einen erzeugen:  python -c "import secrets; print(secrets.token_urlsafe(24))"
#VORLAUT_DEVICE_TOKEN=hier-einen-langen-zufallswert-eintragen
